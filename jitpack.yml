# jitpack.yml

# 最可靠的解决方案：在构建前执行发布任务
jdk:
  - openjdk17

# jitpack.yml

# 使用 Gradle 命令明确构建和发布所有变体
before_install:
  - ./gradlew :loghelper:assembleDevDebug :loghelper:assembleProdRelease :loghelper:publish